first = 'Ellie'
last = 'Stanton'

print(first, last)

name = [
    first,
    last
]

print(name[0], name[1])

dictionary = {
    'first': first,
    'last': last
}

print(dictionary['first'], dictionary['last'])
